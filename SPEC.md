# session-chat 规范

本文件是 `wechat-codex-bridge` 目录会话路由的冻结行为规范。README 负责安装和
使用说明；实现、测试与 README 不得改变这里的安全边界。

## 架构边界

- 腾讯 OpenClaw 微信插件只负责接收、路由和投递。
- Codex 是内部会话、推理和执行单元，不增加中央业务服务。
- SQLite 只保存幂等、绑定事务、批准和回执，不承载业务判断。
- 只处理唯一白名单中的一对一私聊；群聊和非白名单身份在调用 provider 前拒绝。
- OpenClaw、Codex 插件和腾讯微信插件使用仓库锁定版本，不自动升级。

## 目录与持久状态

- 工作根目录 `ROOT` 只是安全边界，不是会话对象，不保存会话绑定。
- `ROOT` 可以与代码目录分离；运行配置必须保存其规范化绝对路径。
- 配置过程把版本化的必需技能安装到 `ROOT/skills/`，不放宽 provider 的技能发现
  准入。
- ROOT 下任意深度、真实存在且安全校验通过的相对目录可以成为会话对象。
- 每个目录只用自己的 `.session-chat.json` 保存绑定，最小字段为：

```json
{
  "schema_version": 1,
  "provider": "codex",
  "session_id": "provider-session-id"
}
```

- provider 与 session ID 必须成对使用，不同 provider 的 ID 不互认。
- 每个授权发送者的 active 会话只存在后端内存中，重启后清空，不写到 ROOT。
- 默认持续使用同一 provider session；压缩和续接由 provider 自身完成，路由层
  不自动轮换。

## 微信协议

只接受三种形式：

```text
@目录路径 正文
@目录路径|new
@目录路径|create
```

- 原始消息字符位置 0 必须是 ASCII `@`；不得 trim，也不得从中间查找路由前缀。
- 普通消息以第一个 ASCII 空格分隔目录路径；后续正文逐字传递，可以包含任意
  `|`、斜杠、`new`、代码和换行。
- 只有整条消息精确匹配 `@非空路径|new` 或 `@非空路径|create` 才是控制命令。
- 路径禁止 `|`；无空格且不精确匹配控制命令的消息是协议错误。
- `/new`、`/create` 和其他命令树没有控制意义。

## 路径安全

- 拒绝绝对路径、ROOT 自身或其别名、越界 `..`、目标类型不符和符号链接逃逸。
- 普通消息与 `|new` 要求目标目录已存在且是目录。
- `|create` 只允许当前不存在的 ROOT 内相对路径；可创建缺失父目录和目标目录，
  但不得覆盖已有目录。
- 目录身份使用规范化后的真实路径。

## 普通消息状态机

- 目录存在且绑定有效：恢复该 session，成功后切换 active 并投递原样正文。
- 目录存在但无配置：有旧 active 时不切换、不写文件，正文回落给旧 active；
  没有旧 active 时失败且不投递。
- 路径不存在：严格失败，不创建、不回落。
- 配置损坏、schema/provider/session ID 无效或 resume 失败：始终失败关闭，保持
  旧 active 且不投递正文。
- provider 返回失败、中断、未接受或未知状态时不得包装成成功；不完整回复不得外发。
- 相同入站消息 ID 只执行一次。

## `|new` 与 `|create`

- 两个命令都必须独立发送且不携带正文。
- `|new`：已有有效绑定时沿用其 provider；无绑定时使用显式
  `default_provider`。provider 无正文创建并确认稳定 ID 后，原子替换配置，再切换
  active。
- `|create`：只针对不存在的安全路径；先创建目录，再用 `default_provider`
  创建首个空 session，原子写配置后切换 active。若后续失败，保留用户可见空目录，
  不偷偷删除。
- 控制命令使用消息幂等和目录/父目录锁；相同消息 ID 不自动重复创建。
- provider 已返回 ID，但后续操作表或配置提交无法可靠确认时，操作进入
  `UNKNOWN_AFTER_PROVIDER_CREATE`；若连标记都不能写入，保留原 `IN_PROGRESS`
  与故障日志。两种情况都保持旧 active/旧绑定、视新 session 为可能 orphan，并
  禁止自动重试。

## Provider 准入

Provider 必须通过以下合同探针后才能启用：

- 无正文创建并返回稳定 session ID；
- 新进程可按 ID 恢复；
- 创建与恢复保持稳定 cwd；
- 可执行单轮并返回明确接受/完成状态；
- 支持长期续接，由 provider 自己处理上下文压缩。

Codex provider 还必须让新建和恢复的目录会话发现仓库内 X/Twitter 技能。该技能
只能复用用户已经登录的同一个 Chrome；读操作可直接执行，点赞、回复、转发、
发布、关注和私信必须绑定一次性、精确且会过期的确认。缺少同一 Chrome 或确认
能力时，X 功能故障关闭，不影响核心目录会话准入。Claude Code 和 PI 不伪装支持。

## 微信出站

- 同一授权私聊的直接回复可自动放行；主动发送、提醒和其他外部动作必须明确批准。
- 直接回复放行只绑定一次、精确目标和精确正文。
- 授权、幂等和存储键使用规范化联系人标识；腾讯渠道投递保留入站事件中的原始
  大小写，以取得正确的 `contextToken`。
- 成功或失败都持久化回执；同一入站编号不自动重复发送。
- 外发调用抛错时可能已经产生副作用；批准保持未知执行状态，禁止自动恢复批准或
  重试，等待人工核对回执。
- 二维码、令牌、账号标识、OAuth 数据、SQLite 和 `.session-chat.json` 不得提交。
