# 变更记录

## 0.2.0 - 2026-07-26

- 新增通用 `session-chat` 目录会话路由，支持 `@路径 正文`、
  `@路径|new` 和 `@路径|create`。
- 新增安全路径解析、目录绑定、发送者内存 active、普通消息幂等，以及
  `UNKNOWN_AFTER_PROVIDER_CREATE` 控制事务恢复边界。
- 接入真实 Codex App Server provider，验证无正文创建、线程元数据持久化、
  跨进程恢复、稳定 cwd 和原生线程续接。
- 将目录路由接入现有 OpenClaw/腾讯微信真实入站链路，并为直接回复加入一次性
  精确放行和成功/失败回执。
- 修复群聊元数据绕过、模糊外发结果重复发送、默认 ROOT 自复制失败，以及 provider
  失败或中断被包装成成功的问题。
- `codex` 模式启动前改为真实执行无正文创建、跨进程恢复、稳定 cwd 和单轮状态
  合同探针，不再用账号登录状态代替合同验证。
- 修复腾讯微信收件人标识大小写被规范化后无法匹配 `contextToken` 的问题。
- 为 Codex 目录会话注入 X/Twitter 技能；缺少同一已登录 Chrome 或一次性写确认
  能力时保持故障关闭。
- 新增 Python 与 Node 测试，覆盖 grammar、路径安全、回落、控制事务、provider
  准入、白名单、确认、回执和真实 Node→Python 调用链。
- 保持 OpenClaw 与插件固定版本，继续关闭自动升级。

## 0.1.0 - 2026-07-25

- 发布固定版本的微信—Codex 单人文本最小闭环。
- 提供中文安装、配置、登录、检查、运行和卸载命令。
- 实现白名单、入站幂等、一次性外发确认和 SQLite 回执。
